# Copyright (c) 2025-2026 Guy Erreich
#
# SPDX-License-Identifier: MIT
name: License Header Check

on:
  push:
    branches: ["master", "staging", "dev"]
  pull_request:
    branches: ["master", "staging", "dev"]
  workflow_dispatch:

concurrency:
  group: license-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  license-check:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # Docker + digest pin: apache/skywalking-eyes is a composite with nested tag pins
      # (e.g. setup-go@v5), which fail under sha_pinning_required. See #263.
      - name: Check license headers
        run: |
          docker run --rm \
            -v "$PWD:/github/workspace" \
            -w /github/workspace \
            apache/skywalking-eyes:0.8.0@sha256:dd138eb0277407ecfcf0beebf2157c5fa092413a2c4041c162e1df87bd3716e8 \
            header check -c .licenserc.yaml
